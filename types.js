const name = 'wes';
const middle = "topher";
const last = `bos`;
const sentence = 'she\'s soo cool';
const song = `oh
i
like
pizza`;
const hello = "hello my name is " + last + ". Nice to meet you";
const html = `
   <div>
   <h2>${name}</h2>
   <p>${hello}</p>
   </div>
`;
console.log(html);