const name = 'wes';
const middle = "topher";
const last = `bos`;
const sentence = 'she\'s soo cool';
const song = `oh
i
like
pizza`;
const hello = "hello my name is " + last + ". Nice to meet you";
const html = `
   <div>
   <h2>${name}</h2>
   <p>${hello}</p>
   </div>
`;
console.log(html);

const age = 100;
const name = 'wes';

const a = 10;
const b = 20;

const myname = 'shruti';
const myfathersname = 'ravindra';
const mymothersname = 'vandana';

const sentence = `hello my name is ${myname}. my father's name is ${myfathersname}. my mother's name is ${mymothersname} `;
console.log(`Sentence ${sentence}`);

const gems = 20;
const kids = 3;
const eachkidgets = Math.floor(gems/kids);
const dadgets = gems % kids;
console.log(`each kid gets ${eachkidgets}`);

const person = {
    first: 'shruti',
    last: 'tripathi',
    age: 18
}; 